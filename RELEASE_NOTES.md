# v0.1.4 — 확정 그림체와 비교군 관리

## 그림체 관리 개편

- 그림체 관리를 `제작 기록`, `확정 그림체`, `공유 그림체`로 나눴습니다.
- 제작 기록은 같은 작가 조합으로 여러 번 생성해도 결과 이미지를 각각 별도 카드로 보관합니다.
- 제작 기록과 공유 그림체를 확정 그림체로 옮길 때 작가·퀄리티·고정·네거티브 프롬프트와 생성 설정을 확인하고 수정할 수 있습니다.
- 확정 그림체에 이미지를 직접 끌어 놓거나 붙여 넣어 추가할 수 있습니다. 이미지에서 읽지 못한 모델·샘플러·스케줄러·Steps·CFG Scale·CFG Rescale·Variety+ 정보는 직접 입력할 수 있습니다.
- 기존 데이터에 생성 설정이 없어도 오류 없이 열리며, 확정 그림체의 모델과 생성 설정을 나중에 수정할 수 있습니다.
- 세 목록 모두 검색·필터·페이지당 표시 수·그림 크기 조절을 지원하고, 공유 그림체는 실제 이미지가 있는 항목만 표시합니다.

## 비교군 관리

- 상단의 `비교군 관리`에서 확정 그림체 여러 개를 골라 같은 캐릭터·고정 프롬프트로 비교 이미지를 생성할 수 있습니다.
- 선택 화면을 그림체 갤러리, 상세 정보, 공통 프롬프트·생성 설정의 3열 구조로 구성했습니다. 좌우 패널은 접어서 갤러리를 더 넓게 볼 수 있습니다.
- 각 그림체에 저장된 모델, 샘플러, 스케줄러, Steps, CFG Scale, CFG Rescale, Variety+를 사용하고, 저장 정보가 없는 경우에만 비교군 기본값을 사용합니다.
- 캐릭터 프롬프트를 여러 줄로 추가·삭제할 수 있고 공통 시드는 미지정, 직접 입력, 첫 번째 생성 이미지 시드 재사용 중에서 선택할 수 있습니다.
- 비교군 목록을 폴더형 갤러리로 표시합니다. 폴더를 열면 생성 결과가 나오고, 선택한 그림과 프롬프트·생성 정보를 오른쪽에서 크게 확인할 수 있습니다.
- 개별 비교 이미지를 삭제하거나 `재습득`으로 다시 생성할 수 있으며, 비교군 자체도 삭제할 수 있습니다.
- 생성 중 `완료 수 / 전체 수`를 이미지마다 실시간으로 갱신합니다. 중간에 실패해도 이미 완성된 결과는 그대로 남습니다.

## 프롬프트와 NovelAI 생성 수정

- 숫자로 끝나는 태그의 닫는 구분자를 NovelAI 형식에 맞게 자동 보정합니다. 예: `1.5::artist:matrix16::` → `1.5::artist:matrix16 ::`.
- 새 프롬프트 생성뿐 아니라 공유 그림체 가져오기, 확정 그림체 저장·불러오기, 기존 데이터 읽기, 실제 NovelAI 요청 직전에도 같은 보정을 적용합니다.
- 비교군 생성 요청을 그림체 제작과 같은 NovelAI V4/V4.5 요청 형식으로 통일하고, V4.5 Full을 포함한 저장 모델과 스케줄러·샘플러 설정을 정확히 전달합니다.
- NovelAI 500 오류에는 추적 가능한 요청 ID를 함께 표시합니다.
- 그림체 제작의 퀄리티·네거티브 랜덤 변경과 제작 이미지·생성 설정의 확정 그림체 이동을 수정했습니다.

## 업데이트

- v0.1.1 이상 사용자는 리모콘의 `업데이트 확인` → `업데이트 받기`로 설치할 수 있습니다.
- v0.1.0 사용자는 프로그램을 완전히 종료한 뒤 새 EXE로 수동 교체해 주세요.
- 기존 개인 평가, 확정 그림체, 생성 기록과 설정은 유지되며 새 필드가 없는 데이터도 자동 호환됩니다.
- 개인 평가, NovelAI App Key, 생성 이미지와 로그인 정보는 배포 파일에 포함되지 않습니다.
- EXE SHA-256: `e5d033fee01c39d9dcabeee6283464e3c695e51c9835b42db81af3f9433977b1`

# v0.1.3 — 그림체 제작·관리 대규모 개선

## 그림체 제작

- 고정 작가를 전체 작가 수에 포함하도록 변경했습니다. 예를 들어 전체 7명 중 고정 작가가 5명이면 남은 2명만 평가 작가 또는 공유 그림체에서 선택합니다.
- 추가할 작가 입력란에서 `0.1::artist:작가명::` 형식을 쉼표나 줄바꿈으로 여러 개 한꺼번에 입력할 수 있습니다.
- 사용자 그래프의 자리별 가중치를 실제 전체 프롬프트 위치에 맞춰 정확히 적용하도록 수정했습니다.
- 평가 작가 태그별 인원 모달을 추가했습니다. 여러 수집 태그마다 선택 인원을 따로 정하고, 남은 자리는 전체 평가 작가에서 무작위로 채울 수 있습니다.
- 특정 수집 태그로 평가한 작가를 태그 지정 자리와 전체 평가 작가 자리에서 제외하는 기능을 추가했습니다.
- 평가 태그 입력에도 Danbooru 태그 자동완성을 지원합니다.
- 수집 그림체 프롬프트의 자동/수동 선택 상자를 제거하고, 퀄리티·네거티브 프롬프트는 `매 장 랜덤 변경` 버튼을 기준으로 동작하도록 단순화했습니다.
- 작가 → 퀄리티 → 고정 태그 순서로 NovelAI 프롬프트를 구성합니다.

## 평가와 작가 뽑기

- 새 후보 수집이 끝나면 첫 작가의 그림을 즉시 표시합니다.
- 준비한 후보를 모두 확인했을 때 자동으로 다시 수집하지 않고 확인 모달을 표시합니다.
- 평가 관리의 수정 화면에서 쿼리 프롬프트를 변경하거나 완전히 비울 수 있습니다. 저장한 내용은 태그별 작가 선택과 제외에도 반영됩니다.

## 그림체 관리

- 그림체 목록 영역과 미리보기 이미지를 확대하고, 선택한 이미지의 상세 정보를 오른쪽에 표시하도록 화면을 재구성했습니다.
- 상세 정보는 `작가 · 퀄리티`, `캐릭터`를 기본 표시하고 네거티브 프롬프트는 토글로 확인할 수 있습니다.
- 작가 정보도 번호 목록이 아니라 NovelAI 가중치 프롬프트 형식으로 표시합니다.
- 여러 그림체를 선택해 생성 이미지와 함께 한꺼번에 삭제할 수 있습니다. 삭제 대상으로 고른 그림체도 오른쪽에서 다시 검토할 수 있습니다.

## 생성 안전성과 화면 개선

- Opus 무료 이미지 기준인 28스텝·최대 1,048,576픽셀을 벗어나면 1장 생성과 연속 생성 전에 Anlas 차감 가능성을 경고합니다.
- 브라우저 기본 확인창을 제거하고 후보 재수집, 평가 삭제, 그림체 삭제, App Key 삭제, 그룹 이름 변경 등에 공통 디자인 모달을 적용했습니다.
- 그림체 제작 영역이 길어지면 내부에서 자연스럽게 스크롤되도록 레이아웃을 개선했습니다.

## 업데이트

- v0.1.1 이상 사용자는 리모콘의 `업데이트 확인` → `업데이트 받기`로 설치할 수 있습니다.
- v0.1.0 사용자는 프로그램을 완전히 종료한 뒤 새 EXE로 수동 교체해 주세요.
- 개인 평가, NovelAI App Key, 생성 이미지와 로그인 정보는 배포 파일에 포함되지 않습니다.
- EXE SHA-256: `f5baa52146b0957da39d6190c96c8cf13a4d486ce25a1fa733b1150c486be9d8`

# v0.1.2 — 고정 태그와 자동완성

## 수정 사항

- 퀄리티 프롬프트 다음에 항상 들어가는 고정 태그 입력란을 추가했습니다.
- 퀄리티·고정·네거티브·캐릭터 프롬프트에서 Danbooru 태그 자동완성을 지원합니다.
- 작가 태그 자동완성 결과를 `artist:작가명` 형식으로 입력합니다.
- 숫자로 끝나는 작가 태그 뒤에 필요한 공백을 자동으로 넣습니다.

# v0.1.1 — 그림체 설정 편집 개선

## 수정 사항

- 추가한 고정 작가를 작가 수 기준 1~12번 모든 자리로 이동할 수 있도록 수정
- 작가를 추가하거나 삭제해도 사용자가 지정한 전체 작가 수가 임의로 바뀌지 않도록 수정
- 그림체 설정을 `작가 구성`, `허용 평점`, `가중치 설정`, `가중치 표` 단위로 각각 접고 펼칠 수 있도록 개선
- 가중치 표의 중첩 스크롤을 제거하고 그림체 설정 본문 전체가 스크롤되도록 개선
- 업데이트 설치 전에 실행 중인 로컬 서버를 정상 종료해 EXE 잠금을 해제
- 리모콘을 강제 종료하지 않고 정상 종료해 PyInstaller 임시 폴더 경고 방지
- 서버와 새 리모콘을 독립된 PyInstaller 환경으로 실행해 삭제된 `_MEI` 임시 폴더 참조 방지
- EXE 교체가 잠금 때문에 실패하면 최대 30초 동안 재시도하고, 성공한 뒤에만 새 버전을 실행

# v0.1.0 — 첫 공개 배포

Danbooru 작가 평가, 그림체 프롬프트 제작, NovelAI 이미지 생성, 공유 그림체 수집·분석을 하나의 Windows 로컬 앱에서 사용할 수 있는 첫 공개 배포판입니다.

## 주요 기능

- Danbooru 작가 검색·샘플 확인·평가 및 수동 작가 관리
- 평가 작가와 공유 그림체 작가를 조합한 NovelAI 그림체 제작
- 공유 그림체 작가의 최소·최대 인원을 지정하는 범위 랜덤 및 공유 작가 전용 구성
- 작가·가중치·퀄리티·네거티브 프롬프트 독립 랜덤 생성
- 기본 프롬프트의 작가/퀄리티 태그 제외·복원 및 고정 편집
- 공유 그림체 게시글 수집, 직접 URL 추가, 정렬·통계·프롬프트 분석
- 수집 프롬프트 인덱스를 이용한 빠른 추천
- Chrome 확장 프로그램을 이용한 현재 아카라이브 로그인 연결
- Windows 리모콘에서 서버 시작·종료, 사이트 열기, 업데이트 확인
- 서버 시작 시 사이트 자동 열기 선택 저장
- 리모콘 X 버튼으로 종료할 때 실행 중인 로컬 서버도 함께 종료

## 처음 실행할 때

배포본에는 공유 그림체 이미지 파일이 들어 있지 않습니다. `용량·시간 확인`은 로컬 DB에서 누락 장수·예상 용량·사이트 제한을 포함한 예상 시간을 즉시 계산합니다. 결과를 확인한 뒤 `다운로드 시작`을 누르면 아직 유효한 주소를 먼저 사용하고, 만료된 주소만 고정 원본 게시글에서 게시글별로 갱신한 직후 EXE 옆 `data\arca_style_images`에 저장합니다. ac.namu.la의 429 제한을 피하는 1.1초 요청 간격과 자동 백오프 재시도를 적용해 초반 일부만 받은 뒤 수동으로 반복해야 하던 문제를 수정했습니다.

공유 그림체 이미지 1,687장을 Google Drive ZIP으로 빠르게 설치하는 경로를 추가했습니다. 3.10 GB ZIP은 중단 후 이어받을 수 있고, 전체 SHA-256·매니페스트·파일별 SHA-256·안전한 압축 경로를 모두 확인한 뒤 설치합니다. 자동 다운로드가 실패하면 사용자가 Drive에서 받은 ZIP 사본을 `내 ZIP 설치`로 선택할 수 있으며, 기존 아카라이브 직접 다운로드 기능은 유지됩니다.

기존 사용자 DB의 내부 행 번호가 배포 DB와 달라도 게시글 주소와 안정 이미지 주소로 1,687장을 정확히 연결하도록 수정했습니다. 이미지가 모두 준비되면 ZIP 설치와 아카라이브 직접 받기 영역은 자동으로 접혀 화면을 최소화합니다.

그림체 제작에서 공유 그림체 작가 인원을 최소~최대로 지정할 수 있습니다. 두 값을 같게 하면 고정 인원이 되며, 전체 작가 수 12명에 최소 12명·최대 12명을 지정하면 개인 평가 작가 없이 공유 그림체 작가 12명만으로 구성할 수 있습니다. 개인 평가 작가가 부족한 경우에도 지정한 최대 범위 안에서 공유 작가를 더 선택해 전체 인원을 채웁니다.

성인 게시판의 이미지는 로그인이 필요할 수 있습니다. 수집 화면의 안내에 따라 포함된 Chrome 확장 프로그램을 처음 한 번 등록한 뒤 현재 로그인을 연결할 수 있습니다.

## 배포 데이터와 개인정보

- NovelAI App Key, 브라우저 쿠키, 로그인 프로필, 생성 이미지, 로컬 이미지 경로는 포함되지 않습니다.
- 개인 작가 평가 데이터인 `ratings`는 포함되지 않으며 처음 실행 시 평가 목록은 비어 있습니다.
- 공유 그림체 게시글·프롬프트·이미지 원본 URL과 공개 Danbooru 작가 수 캐시만 정제된 시드 DB로 포함됩니다.
- 실제 공유 그림체 이미지는 사용자가 전용 버튼을 누를 때 별도로 받습니다.
